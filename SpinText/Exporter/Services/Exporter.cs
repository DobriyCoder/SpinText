using SpinText.HTProvider.DB;
using SpinText.HTProvider.Models;
using System.IO;

namespace SpinText.Exporter.Services;

public class Exporter
{
    /*TODO: IFileStream*/ object CreateExportHTFile(HTData[] data)
    {
        return default;
    }

    /*TODO: IFileStream*/ object CreateLogFile(HTGeneratedLogData data)
    {
        return default;
    }
}
