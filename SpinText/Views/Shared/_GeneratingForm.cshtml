@model IPartialModel

<div class="dc-generatingform @Model?.Cls" @Model?.Attrs>
    <form asp-action="AddHT">
        <tpl :CurrencyPairs />
        <tpl :ProgressBar />
        <tpl :StatusBar />
        <tpl :CtrlBtn />
    </form>
</div>